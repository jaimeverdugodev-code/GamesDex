{
  "buildCommand": "node scripts/set-env.js && npm run build",
  "outputDirectory": "www",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
